/**
 * Establish connection with reciever. Send packets delay depending of the 
 * network configuration. Manage collision, resend on error. Produce stats
 * on transmitted packets.
 */
var SDR = function () {
	return {
		
	};
}();